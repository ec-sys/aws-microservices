apiVersion: v1
kind: Service
metadata:
  name: api-gateway
spec:
  type: ClusterIP
  selector:
    app: api-gateway-pod
  ports:
    # Service only exposes the Envoy container
    - name: envoy-port
      protocol: TCP
      port: 8080
      targetPort: envoy-port