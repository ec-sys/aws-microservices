apiVersion: skaffold/v4beta5
kind: Config
metadata:
  name: aws-service-faker
build:
  artifacts:
    - image: longdd90jp/aws-service-faker
      docker:
        cacheFrom:
          - longdd90jp/aws-service-faker
  tagPolicy:
    gitCommit: {}
manifests:
  rawYaml:
    - k8s/**.yaml
