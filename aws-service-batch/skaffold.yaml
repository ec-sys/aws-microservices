apiVersion: skaffold/v4beta5
kind: Config
metadata:
  name: aws-service-batch
build:
  artifacts:
    - image: longdd90jp/aws-service-batch
      docker:
        cacheFrom:
          - longdd90jp/aws-service-batch
  tagPolicy:
    gitCommit: {}
manifests:
  rawYaml:
    - k8s/**.yaml
