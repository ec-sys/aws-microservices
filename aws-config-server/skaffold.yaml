apiVersion: skaffold/v4beta5
kind: Config
metadata:
  name: aws-config-server
build:
  artifacts:
    - image: longdd90jp/aws-config-server
      jib:
        args:
          - -DskipTests
  tagPolicy:
    gitCommit: {}
manifests:
  rawYaml:
    - k8s/**.yaml
