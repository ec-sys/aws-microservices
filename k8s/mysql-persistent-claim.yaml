apiVersion: v1
kind: PersistentVolume
metadata:
  name: db-pv-envov
  labels:
    name: db-pv-envov
spec:
  # storage name use in pvc
  storageClassName: db-pv-envov
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    # storage path in cluster
    path: "/data/pv0001/"

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  labels:
    name: mysql-pvc
spec:
  storageClassName: db-pv-envov
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi