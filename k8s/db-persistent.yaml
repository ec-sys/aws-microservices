apiVersion: v1
kind: PersistentVolume
metadata:
  # ten cua pv
  name: db-pv
  labels:
    name: db-pv
spec:
  # class name pv de dung o pvc
  storageClassName: db-pv-zen-l1qaqayhbNKkml,nuj
  capacity:
    storage: 5Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    # data luu o cluster
    path: "/data/pv0001/"

---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  # ten cua pvc
  name: db-pvc-mysql
  labels:
    name: db-pvc-mysql
spec:
  # class name cua pv de claim
  storageClassName: db-pv-mysql
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1.5Gi
